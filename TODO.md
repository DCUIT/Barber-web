# TODO
- [ ] Fix backend-node/package.json to be valid JSON after bcrypt->bcryptjs change
- [ ] Update package-lock.json (regenerate) by running npm install in backend-node
- [ ] Ensure no leftover `bcrypt` imports remain
- [ ] Run backend locally (node src/server.js) to verify startup
- [ ] Deploy again to Render

